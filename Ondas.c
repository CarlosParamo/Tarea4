#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
	FILE *infile;
	infile = fopen("cond_ini_cuerda.dat", "w");
	int N=129
      
	int Array[N];
	int i;

	for(i=0; i<N; i++)
	{	
		fscanf(infile,"%1d",&

